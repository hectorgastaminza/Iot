<project name="AntJavaProject" default="main" basedir=".">
	<description>
		Ant build script for Comiot Device Client
	</description>

	<property name="projectName" value="COMIOT_DeviceClient" />
	
	<!-- Java sources -->
	<property name="src.dir" location="src" />
	
	<!-- Java classes -->
	<property name="build.dir" location="bin" />
	
	<!-- Output, Jar -->
	<property name="dist.dir" location="dist" />
    
    <path id="class.path">
      <fileset dir="${src.dir}/lib">
        <include name="**/*.jar" />
      </fileset>
    </path>

	<target name="init">
		<!-- Create the time stamp -->
		<tstamp />
		<copy todir="${src.dir}">
          <fileset dir="../../../java"/>
        </copy>
        <copy todir="${src.dir}/lib">
          <fileset dir="../../../../../library"/>
        </copy>
        <delete dir="${src.dir}/unitTest"/>
        <delete dir="${src.dir}/demo/server"/>
        <delete dir="${src.dir}/demo/raspberry"/>
        <delete dir="${src.dir}/device/raspberry"/>
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build.dir}" />
	</target>

	<target name="compile" depends="init" description="compile the source ">
		<!-- Compile the java code from ${src.dir} into ${build.dir} -->
		<javac includeantruntime="false" srcdir="${src.dir}" destdir="${build.dir}">
            <classpath refid="class.path" />
        </javac>
	</target>

	<target name="dist" depends="compile" description="package, output to JAR">

		<!-- Create the distribution directory -->
		<mkdir dir="${dist.dir}" />

		<!-- Put everything in ${build} into the {$projectName}-${DSTAMP}.jar file -->
		<jar jarfile="${dist.dir}/${projectName}-${DSTAMP}.jar" basedir="${build.dir}">
		    <manifest>
			<!-- create an executable Jar -->
                <attribute name="Main-Class" value="demo.client.Main" />
                <attribute name="Class-Path" value="${jar.classpath}" />
		    </manifest>
		</jar>
        <copy todir="${dist.dir}">
          <fileset dir="${src.dir}/lib"/>
        </copy>
        <!-- 
        <delete dir="${src.dir}" />
		<delete dir="${build.dir}" />
		-->
	</target>

	<target name="clean" description="clean up">
	    <delete dir="${src.dir}" />
		<delete dir="${build.dir}" />
		<delete dir="${dist.dir}" />
	</target>
	
	<!-- Default, run this -->
	<target name="main" depends="clean, compile, dist" />
	
</project>